/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['static.wixstatic.com', 'another-domain.com', 'yet-another-domain.com'],
    },
  };
  
  export default nextConfig;
  